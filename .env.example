# Environment variables template.
# Copy to `.env` (which is gitignored) and fill in real values locally.
# NEVER put real secrets in this file — it is committed to version control.
#
# No environment variable references were detected in the codebase yet.
# Add entries as the project grows, one per line, with a brief comment:
#
# Example:
# API_BASE_URL=        # base URL for the upstream API
# API_TOKEN=           # secret token — set locally, do not commit
